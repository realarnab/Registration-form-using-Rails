<div id="<%= dom_id(profile) %>" class="profile-card" style="border: 1px solid #ddd; padding: 15px; margin-bottom: 20px;">
  <p>
    <strong>Name:</strong> <%= profile.name %>
  </p>
  <p>
    <strong>Age:</strong> <%= profile.age %>
  </p>
</div>
